from setuptools import setup, find_packages

setup(
    name='',
    version='0.1.0',
    description='',
    author='',
    author_email='',
    packages=find_packages(),
    install_requires=[
        # List any dependencies here
    ],
)
